{
  "name": "adaptlearn-lms",
  "version": "1.0.0",
  "description": "AURA AdaptLearn LMS — THPT Thủ Thiêm on-premise deployment",
  "private": true,
  "workspaces": [
    "packages/lms-api",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/lms-api & npm run dev --workspace=packages/frontend",
    "build": "npm run build --workspace=packages/lms-api && npm run build --workspace=packages/frontend",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:migrate": "npm run db:migrate --workspace=packages/lms-api",
    "db:seed": "npm run db:seed --workspace=packages/lms-api"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "author": "THPT Thủ Thiêm — EdTech Team",
  "license": "UNLICENSED"
}
